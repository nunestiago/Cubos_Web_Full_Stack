import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  root: {
    marginLeft: 60,
    marginTop: 78,
  },
  divider: {
    margin: '1rem 0',
  },
}));

export default useStyles;
