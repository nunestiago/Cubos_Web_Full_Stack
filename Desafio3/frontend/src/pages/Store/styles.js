import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  root: {
    marginLeft: 60,
    marginTop: 78,
  },
  title: {
    marginBottom: 86,
  },
  subtitle: {
    marginBottom: 57,
  },
  divider: {
    margin: '1rem 0',
    width: '100%',
  },
}));

export default useStyles;
